# Phase 2 — Confirmed Error Register

Adversarially-verified findings from the Phase-2 audit (51 confirmed). Each item is a Phase-3 fix candidate. Source: `STORIES.csv` test_status/errors columns.

**Severity:** high 9 · medium 18 · low 24  
**Type:** logistical 24 · ux 14 · spec-error 6 · a11y 4 · bug 3

## HIGH severity (9)

### [ ] F-C18-07 · AI-range labeling mark / decoration `logistical`

- **Cluster:** C18 · **Verdict:** fail
- **Problem:** Story expects an aiLabel mark/decoration applied to AI-edited ranges (per agents.md I.1 ai-labeling), exposed via buildAiExtensions(options) and gated by decorateAiRanges. The implementation is a stub: buildAiExtensions returns an empty Extensions array unconditionally and ignores its options; no mark, decoration, or apply/remove command exists. decorateAiRanges is declared but never read. The AIPanel only renders a static 'AI Editor' text badge that is not tied to any edited document range, so AI changes are visually indistinguishable from user edits in the editor surface. The story row itself is marked 'stub'.
- **Fix:** Implement an aiLabel mark (or ProseMirror decoration plugin) in schema/ai.ts with apply/remove commands, return it from buildAiExtensions when decorateAiRanges is true, and apply the mark/decoration over the ranges of an applied EditPatch in handleApplyAiPatch so AI ranges are visually labeled and filterable. NOTE: buildAiExtensions is ALREADY wired into createLashEditorExtensions (packages/editor-core/src/schema/index.ts:38 spreads buildAiExtensions(options?.ai)), so no new registration is needed -- only the extension body and the apply-on-patch call.
- **Verified:** CONFIRMED with a correction to the suggested*fix. schema/ai.ts:22 is a verbatim stub: buildAiExtensions = (\_options?) => []; the options param is prefixed * and never read, decorateAiRanges (line 18) is declared but never consumed anywhere, and there is no mark/decoration/command in the file or repo (grep for aiLabel/aiRange returns only the stub doc-comment). AIPanel.tsx:233-235 renders a static 'AI Editor' badge unrelated to any document range, and handleApplyAiPatch (EditorWorkspace.tsx) applies the patch text via setContent with no range labeling. STORIES.csv row F-C18-07 is explicitly status 'stub'. So the verdict 'fail', issue_type 'logistical' (declared-but-unimplemented feature), and severity 'high' are all justified. The ONE inaccuracy in the original suggested_fix: it says to 'register it in createLashEditorExtensions', but schema/index.ts:13,38 already imports and spreads buildAiExtensions(options?.ai) into the extension list -- registration exists; only the empty return value and the apply-time labeling are missing. I corrected the fix text accordingly.

### [ ] F-C23-01 · Distributed tracing client `logistical`

- **Cluster:** C23 · **Verdict:** fail
- **Problem:** The Span and Tracer interfaces are declared, but createTracer is a pure stub that unconditionally throws. No span is ever started, ended, recorded, or exported, and no service in the monorepo imports it. The tracing capability does not exist beyond type definitions.
- **Fix:** Implement createTracer to return a Tracer whose start() captures performance.now()/Date.now() into a Span (with name, attrs, parentId) and whose end() stamps endMs and forwards the completed span to an exporter (e.g. OTLP or a pluggable sink). Then wire it into the realtime-gateway/web request paths so traces are actually emitted.
- **Verified:** CONFIRMED independently. Read packages/observability/src/index.ts: lines 15-18 declare the Tracer interface; line 33-35 createTracer throws new Error('createTracer: not implemented (M5/F5)'). A codebase-wide grep for '@lash/observability' and 'createTracer' (excluding the file itself) returned zero hits; the only 'observability' match elsewhere is an unrelated Cloudflare wrangler config in a test. STORIES.csv line 173 corroborates (stub/story-drafted). Note: the original evidence line numbers in the finding (lines 6-19, 31-33) are slightly off from the actual file (interfaces 6-18, throw at 33-35), but the substance is correct. issue_type 'logistical' is defensible since the package is honestly self-labeled 'SCAFFOLD — implement in M5/F5' (roadmapped, not yet built) rather than mis-behaving; severity high is appropriate because the capability is entirely absent. suggested_fix is sound.

### [ ] F-C23-02 · Metrics recording (counters/histograms) `logistical`

- **Cluster:** C23 · **Verdict:** fail
- **Problem:** The MetricRecorder interface (inc/observe) is declared, but createMetricRecorder unconditionally throws. No counter is incremented and no histogram observation is aggregated or exported; nothing is wired into any service, so SLO monitoring is impossible.
- **Fix:** Implement createMetricRecorder backed by a real metrics registry (e.g. prom-client Counter/Histogram or OTLP metrics) keyed by serviceName, so inc()/observe() update and export metrics. Add tests and instrument at least one service path.
- **Verified:** CONFIRMED independently. MetricRecorder interface at lines 20-25 (inc/observe); createMetricRecorder at lines 37-39 throws new Error('createMetricRecorder: not implemented (M5/F5)'). No external consumers found via grep. STORIES.csv line 174 corroborates. Same caveat as F-C23-01 on minor line-number drift in the original evidence. severity high and issue_type logistical retained; suggested_fix is sound.

### [ ] F-C23-03 · Structured logging client `logistical`

- **Cluster:** C23 · **Verdict:** fail
- **Problem:** The Logger interface (info/warn/error with attrs) is declared, but createLogger unconditionally throws. There is no logging backend or output of any kind, and no callers exist, so operators cannot investigate incidents via this client.
- **Fix:** Implement createLogger returning a Logger that emits structured JSON lines (level, msg, serviceName, attrs, timestamp) to stdout or a pluggable transport (e.g. pino), and adopt it across services.
- **Verified:** CONFIRMED independently. Logger interface at lines 27-31 (info/warn/error each taking msg + attrs); createLogger at lines 41-43 throws new Error('createLogger: not implemented (M5/F5)'). No callers found via grep. STORIES.csv line 175 corroborates. severity high and issue_type logistical retained; suggested_fix is sound.

### [ ] F-C23-04 · SLO budgets / SLO alignment `logistical`

- **Cluster:** C23 · **Verdict:** fail
- **Problem:** The story expects SLO budgets to be defined and tracked so error budgets gate releases. The module only contains an aspirational header comment claiming alignment 'with the SLOs in agents.md' and a 'SCAFFOLD — implement in M5/F5' note. There is no SLO target, error budget, burn-rate calculation, threshold, or any gating logic anywhere in the package.
- **Fix:** Add an SLO module defining objectives (target, window), error-budget computation from the metrics recorder, and a burn-rate/gate API consumable by CI to block releases when the budget is exhausted; back it with real recorded metrics once F-C23-02 is implemented.
- **Verified:** CONFIRMED independently. The entire file is 44 lines: a header comment (lines 1-4) referencing 'the SLOs in agents.md' and labeling the module 'SCAFFOLD — implement in M5/F5', three interfaces (lines 6-31), and three throwing factory stubs (lines 33-43). There is no SLO target, window, error-budget, burn-rate, threshold, or gating logic anywhere in the package — verified by full read of the only source file. STORIES.csv line 176 corroborates (classified 'missing'). suggested_fix is sound and correctly notes the dependency on F-C23-02 (metrics) being implemented first. severity high retained; issue_type logistical retained given the roadmapped-scaffold nature.

### [ ] F-C24-01 · Postgres document store `logistical`

- **Cluster:** C24 · **Verdict:** fail
- **Problem:** The DocStore interface (load/save keyed by docId and resultSha) is declared, but createPostgresDocStore is a stub that unconditionally throws. There is no DB connection, no SQL, and no persistence. Documents are not saved to or loaded from Postgres at all, so no state survives a restart. No consumer in the repo imports it either, so the feature is entirely absent.
- **Fix:** Implement createPostgresDocStore with a real pg client: create a documents table (doc_id PK, doc jsonb, result_sha text), implement save() as an upsert and load() as a SELECT returning {doc, resultSha} or null. Add an integration test and wire at least one consumer (e.g. realtime-gateway persistence).
- **Verified:** CONFIRMED. index.ts:11-14 declares DocStore; index.ts:41-43 is createPostgresDocStore = () => { throw new Error('createPostgresDocStore: not implemented (M2/C2)') }. No connection/SQL/persistence. Repo-wide grep for DocStore/@lash/storage outside this file returns only the tsconfig path alias and the package.json self-name — no importer. STORIES.csv row F-C24-01 is tagged 'stub'. Severity high and issue_type logistical are appropriate (whole feature absent, not a behavioral bug). Suggested fix is sound.

### [ ] F-C24-02 · Append-only Postgres history log with parent-sha precondition `logistical`

- **Cluster:** C24 · **Verdict:** fail
- **Problem:** HistoryLogStore declares append() with optimistic-concurrency (returns {ok:false, reason:'parent-mismatch', currentHead}), list() with since/until/authorId filters, and headSha(). createPostgresHistoryStore throws unconditionally. The CAS/parent-mismatch logic exists only as a TypeScript signature; no table, query, or concurrency check is implemented, so concurrent writes are neither detected nor rejected.
- **Fix:** Implement createPostgresHistoryStore: history table keyed by doc_id with append-only rows, headSha() = latest row's result_sha. append() should run a transactional CAS (compare expectedParentSha to current head; on mismatch return {ok:false, reason:'parent-mismatch', currentHead}). Implement list() filters as parameterized WHERE clauses (use placeholders, never string interpolation).
- **Verified:** CONFIRMED. index.ts:16-25 declares HistoryLogStore including the parent-mismatch CAS return shape and list() filters; index.ts:45-47 throws unconditionally. No table/query/concurrency logic exists; the CAS is type-only. STORIES.csv F-C24-02 tagged 'stub'. Severity high and logistical correct. Suggested fix is sound; the parameterized-query note is good hygiene guidance for when it is implemented.

### [ ] F-C24-03 · S3 object store with signed URLs `logistical`

- **Cluster:** C24 · **Verdict:** fail
- **Problem:** ObjectStore declares put/get/signUrl, but createS3ObjectStore throws unconditionally. No AWS SDK usage, bucket access, or URL signing exists. Binary objects cannot be stored or retrieved, and no time-limited signed URLs can be generated. There is also no consumer wiring uploads to this store.
- **Fix:** Implement createS3ObjectStore using @aws-sdk/client-s3 + @aws-sdk/s3-request-presigner: put() -> PutObjectCommand returning the object URL, get() -> GetObjectCommand returning Uint8Array or null on NoSuchKey, signUrl() -> getSignedUrl with expiresIn=ttlSeconds. Add an upload consumer for media/attachments.
- **Verified:** CONFIRMED. index.ts:27-31 declares ObjectStore; index.ts:49-51 throws 'createS3ObjectStore: not implemented (M5)'. No AWS SDK import, no signing, no consumer. STORIES.csv F-C24-03 tagged 'stub'. Severity high and logistical correct. Suggested fix is sound.

### [ ] F-C24-04 · Search index hydration and full-text search `logistical`

- **Cluster:** C24 · **Verdict:** fail
- **Problem:** SearchIndex declares hydrate(docId, doc) and search(query, {docIds?}) returning {docId, rangeFrom, rangeTo, snippet}[]. Unlike the other three, there is NO factory function (no create\* for SearchIndex) and no implementation at all — it is purely a type declaration with no concrete behavior and no consumers. Users cannot search documents or get snippet/range results.
- **Fix:** Add a createSearchIndex factory (e.g. Postgres tsvector/GIN-based or an external index) implementing hydrate() to extract and index document text, and search() to run a scoped full-text query returning real character ranges and snippets. Wire it to document save events.
- **Verified:** CONFIRMED. index.ts:33-39 declares the SearchIndex interface and it is the only one of the four with no exported factory (file ends at line 52 with only the three create\* throwing stubs). No createSearchIndex exists; grep finds no SearchIndex usage outside this file. STORIES.csv F-C24-04 is tagged 'missing' (distinct from the other 'stub' rows), corroborating that this is more absent than the others. Severity high and logistical correct. Suggested fix is sound.

## MEDIUM severity (18)

### [ ] F-C01-07 · Left sidebar with navigation, outline, and collapse `ux`

- **Cluster:** C01 · **Verdict:** partial
- **Problem:** Outline and collapse work: collapse toggle flips sidebarCollapsed (aria-pressed), the collapsed 'Outline' access button re-expands, outline is hidden in focus mode, and outline clicks focus/scroll the editor or toggle heading collapse. However PRIMARY_NAV (Current doc, Home, Inbox, Starred, Shared with me) and SECONDARY_NAV (All documents, Settings) are rendered as buttons with NO onClick handler — pure dead controls. 'Current doc' even carries aria-current='page' and data-active, presenting a non-functional control as the active nav target. This is misleading affordance for a shipped UI.
- **Fix:** Either wire the nav items (Home/All documents -> route, Settings -> panel, etc.) or visually mark them as disabled/coming-soon (aria-disabled, muted styling) so users aren't presented clickable-looking buttons that do nothing. At minimum remove the aria-current='page'/active treatment from the dead 'Current doc' entry.
- **Verified:** CONFIRMED. Sidebar.tsx:44-57 — NavItem renders <button type="button"> with only className, data-active, aria-current, and title; no onClick prop is passed and the component signature (NavLink) has no handler field. PRIMARY_NAV (31-37) and SECONDARY_NAV (39-42) are static arrays mapped through NavItem (79-81, 106-108), so all 7 nav buttons are inert. 'Current doc' has active:true (32), which yields aria-current="page" and data-active="true" (50,49) on a button that does nothing — a non-functional control announced as the active page. Contrast: the collapse toggle (113-122), mobile close (124-134), the collapsed outline-access button (84-94 with onClick={onToggleCollapsed}), and the OutlinePanel (97-103) are all correctly wired. Severity medium and issue_type ux are appropriate (misleading affordance, no crash/a11y blocker; arguably borderline a11y given the false aria-current, but ux is the better primary classification). Suggested fix is sound.

### [ ] F-C01-08 · Top bar chrome (title, focus/suggest toggles, share, avatars, mobile menu) `ux`

- **Cluster:** C01 · **Verdict:** partial
- **Problem:** TopBar shows doc icon+title, AutosaveIndicator, focus-mode toggle (aria-pressed, maximize/minimize swap), suggest-mode toggle (aria-pressed, drives the 'Suggesting' meta badge), a Share button that opens the Share rail tab, a mobile menu button, and the extras slot (document select + new-doc button). All wiring is correct. The defect is the 3-avatar collaborator stack: it is hardcoded (Apollo/Ada Lovelace/Grace Hopper) with title='<name> · viewing now', presenting fake live presence as real. A separate, genuinely live presence bar (realtimePresence) exists near the document header, making the top-bar stack redundant and actively misleading.
- **Fix:** Derive the avatar stack from realtimeSnapshot.peers (the same source the live presence bar uses) and render an empty/solo state when there are no remote peers, instead of three hardcoded names announced as 'viewing now'.
- **Verified:** CONFIRMED. TopBar.tsx:27-31 — PRESENT_COLLABORATORS is a hardcoded const array [Apollo/coral, Ada Lovelace/blue, Grace Hopper/green]. It is mapped into AvatarStack (117-126) with title={`${collaborator.name} · viewing now`}, so three fixed fake names are announced as live presence regardless of actual collaborators. TopBar receives no peer/presence prop (props 33-46) so it cannot reflect real state. Independently, EditorWorkspace.tsx:1421 reads remotePeers = realtimeSnapshot.peers and 1426-1491 builds the genuine 'realtime-presence-bar' that maps real peers (1455-1469, data-testid=remote-collaborator) and renders Ready/Invite (1470-1484) or Solo (1485-1488) empty states — confirming a real, separate presence source exists and the top-bar stack is redundant and misleading. The TopBar is otherwise wired correctly (focus/suggest toggles 87-115, share 128-139, mobile menu 62-72, extras 85). Severity medium and issue_type ux are correct (fabricated-presence UX issue, not a functional bug or a11y blocker). Suggested fix is sound: thread realtimeSnapshot.peers into TopBar and reuse the existing empty/solo pattern, or remove the redundant stack entirely.

### [ ] F-C02-07 · Toolbar grouping, icons, and shortcut tooltips `spec-error`

- **Cluster:** C02 · **Verdict:** partial
- **Problem:** Grouping, icons, and tooltips are correct: toolbar.ts:13-26 tags each button group=marks/blocks; EditorToolbar.tsx:29-42 maps all 12 ids to IconName values that all exist in components/shell/Icon.tsx (IconName union lines 34-45: bold/italic/underline/code/link/h1/h2/h3/list-bullet/list-numbered/list-check/table), with a text-glyph fallback (EditorToolbar.tsx:94); shortcutHint (EditorToolbar.tsx:44-53) builds per-platform tooltips. The story's expected_behaviour is mis-derived: it claims hotkeys are display-only and that Mod+K is not wired to the prompt flow. In reality LashKeyboardShortcuts (base.ts:61-85) explicitly wires Mod-b/i/u/e, Mod-Alt-1/2/3, Mod-Shift-8/7/x/t, and Mod-k. Mod-k (base.ts:76-82) calls this.options.onRequestLink when present, i.e. it IS wired to the prompt flow. Separately chip.ts:431-441 ALSO binds Mod-k, returning false when no chip is at the selection so it falls through to the link handler; this shared keybinding is fragile and undocumented.
- **Fix:** Correct the story note: hotkeys are functionally wired via LashKeyboardShortcuts (base.ts:61-85), and Mod+K IS connected to onRequestLink (the prompt flow). Make the Mod-k double-binding between base.ts:76-82 and chip.ts:431-441 explicit — document the chip-first / link-fallback priority (chip returns false when no chip at selection) or consolidate to a single owner — to prevent future regressions.
- **Verified:** CONFIRMED. All code references verified: base.ts:61-85 wires every listed hotkey including Mod-k -> onRequestLink (lines 76-82), directly contradicting the story's 'display-only' and 'Mod+K not wired to prompt' claims; this is a real spec-error in the story. chip.ts:431-441 independently binds Mod-k and returns false via findChipAtSelection guard, confirming the documented-but-fragile double-binding. Icon names confirmed present in components/shell/Icon.tsx (note: the finding cited 'shell/Icon.tsx'; actual path is apps/web/components/shell/Icon.tsx, imported as '../../shell/Icon' — same file, no impact). issue_type spec-error and medium severity are appropriate since the primary deliverable is correcting a wrong story spec plus flagging the keybinding overlap; no functional bug in the shipped toolbar itself.

### [ ] F-C03-04 · Expand all headings `ux`

- **Cluster:** C03 · **Verdict:** partial
- **Problem:** The expandAllHeadings command logic is correct and matches the story's acceptance criteria: at outline.ts:321-339 it reads pluginState.collapsedIntent (the unfiltered intent set, not the doc-filtered visible `collapsed` set), no-ops returning true when the intent set is empty, and otherwise dispatches a 'set-collapsed' meta with collapsedIds:[] which clears all collapse state including ids for headings no longer present in the doc. The command is exposed via lashCommands.expandAllHeadings (commands.ts:60-68). However, it is NOT wired to any user-facing affordance: a grep of apps/web and packages for expandAllHeadings / 'Expand all' / 'expand-all' returned only the two definition sites (commands.ts and outline.ts) and zero callers. OutlinePanel.tsx:25-29 renders only an <h2> 'Outline' title in its header (no expand-all button); the toolbar config (toolbar.ts) has no expand-all entry. A returning user with collapsed sections can only expand them one at a time. The user story's goal ('expand every collapsed heading at once') is unreachable through the UI.
- **Fix:** Add an 'Expand all' button to the OutlinePanel header (next to the 'Outline' <h2> title) that calls a new onExpandAll prop wired in EditorWorkspace to lashCommands.expandAllHeadings(editor); hide or disable it when getCollapsedHeadingIds(state) is empty, and give it an aria-label like 'Expand all sections'.
- **Verified:** CONFIRMED. Independently reproduced all three load-bearing facts: (1) command logic at outline.ts:321-339 is correct and checks collapsedIntent (not the filtered `collapsed`) and clears with []; (2) lashCommands.expandAllHeadings is exposed at commands.ts:60-68; (3) grep over apps/web and packages (excluding tests) found NO 'Expand all' UI control and NO caller of expandAllHeadings outside its own definitions, and OutlinePanel.tsx header (lines 25-29) contains only the title. issue_type=ux and severity=medium are appropriate: the command works but is user-unreachable, a non-blocking gap with an individual-toggle workaround. Caveat: the F-C03-04 acceptance criteria as written in STORIES.csv describes ONLY the command behavior (evidence pointer is solely outline.ts:321-339) and never specifies a button, so by the literal spec the implementation passes; the gap is between the user-story narrative ('at once') and the absence of UI. This makes it a legitimate ux/partial finding rather than a hard fail. The suggested fix is sound; I refined it to reference the existing getCollapsedHeadingIds/state helpers and the EditorWorkspace prop-wiring pattern already used for onToggle/onFocus.

### [ ] F-C05-04 · TSV copy of table cell selection `ux`

- **Cluster:** C05 · **Verdict:** partial
- **Problem:** TSV copy works for a true cell range: handleCopyEvent builds a matrix via extractSelectionMatrix (text content for text cells, value attr for status/select), serializes with buildTsvFromMatrix, preventDefaults, and writes both text/plain and text/tab-separated-values. However hasTableSelection returns true whenever the caret merely sits inside any cell (collectSelectedCells uses cellAround for non-CellSelection and returns the single surrounding cell). So pressing Ctrl/Cmd+C with just a caret — or a partial text selection inside one cell — is hijacked: the entire cell is copied as TSV and the native copy is prevented, clobbering normal in-cell text copy.
- **Fix:** Gate handleCopyEvent on an actual CellSelection (selection instanceof CellSelection) rather than hasTableSelection, so a caret/partial text selection inside a single cell uses native copy.
- **Verified:** CONFIRMED by code. interop.ts:170 hasTableSelection = collectSelectedCells(state).length > 0. utils.ts:24-44: for a non-CellSelection it falls to cellAround(selection.$from) and returns a single {pos,node} for the surrounding cell, so a bare caret inside any table cell yields length 1 -> hasTableSelection true. index.ts:491-504 handleCopyEvent then calls extractSelectionMatrix (which for a caret produces a 1x1 matrix via resolveSelectionGeometry's cellAround branch, width/height=1), event.preventDefault(), and writes TSV. Net effect: a caret or partial in-cell text selection has its native copy suppressed and the entire cell's display value is copied instead of the user's text selection. Severity medium and issue_type ux are appropriate (data-loss-on-copy of selected text is annoying but recoverable). Suggested fix is sound: gating on selection instanceof CellSelection restores native copy for single-cell/caret cases while preserving multi-cell TSV copy.

### [ ] F-C05-05 · TSV/CSV paste into table cell selection `ux`

- **Cluster:** C05 · **Verdict:** partial
- **Problem:** Paste logic is correct: handlePasteEvent reads text/tab-separated-values (falls back to text/plain), parseClipboardTableText splits on tabs if any tab present else commas, trims, normalizes ragged rows to equal columns, and applyMatrixToSelection maps cell-by-cell with positions remapped through tr.mapping.map; text cells get paragraph content replaced, status/select cells accept only valid options else fall back to options[0]; on change it preventDefaults and closes any open picker. Same over-broad gating issue as copy: hasTableSelection is true for a mere caret in a cell, so pasting clipboard text while just typing in a single cell is intercepted and the value is force-overwritten/normalized instead of inserting at the caret.
- **Fix:** Restrict handlePasteEvent to a real CellSelection (or to multi-cell rects) so single-cell caret paste uses default ProseMirror paste; alternatively only intercept when the parsed matrix has >1 cell.
- **Verified:** CONFIRMED by code. index.ts:506-526 handlePasteEvent gates on the same hasTableSelection (interop.ts:170 + utils.ts:35-43 cellAround branch), so a bare caret in a single cell passes the gate. resolveSelectionGeometry (interop.ts:27-47) then yields a 1x1 rect and applyMatrixToSelection (interop.ts:109-163) replaces the whole cell's paragraph content (replaceCellContent, utils.ts:103-114 replaces from cell.pos+1 to end) rather than inserting at the caret; for status/select cells the pasted value is coerced to options[0] if invalid. event.preventDefault() suppresses native paste. This breaks ordinary mid-text paste inside a single cell. Severity medium / ux appropriate. Fix is sound; the alternative ('only intercept when parsed matrix has >1 cell') is weaker because a 1x1 TSV paste into a deliberate single CellSelection would then fall through to native paste — preferring 'selection instanceof CellSelection' is the cleaner gate.

### [ ] F-C09-08 · Hardcoded demo providers/policy in panel `ux`

- **Cluster:** C09 · **Verdict:** partial
- **Problem:** MentionPanel constructs the policy engine and providers entirely from hardcoded stub data: callerId 'local-user', fixed locale en-US, timezone Asia/Tokyo, fixed now '2026-05-16T00:00:00.000Z' (lines 21-26); revocations always false; a visibility rule denying any non-doc target whose refId contains 'secret'; a user search returning only a canned Ada Lovelace; and a group search returning canned Design Team / Secret Group (lines 53-84). There is no backend/API integration for real directories or the real caller identity, so suggestions are fabricated fixture data presented as if real. The fixed `now` makes natural-date parsing (parseDateMention) compute dates relative to 2026-05-16 rather than the actual current time. Additionally there is no loading, empty-result ('no matches'), or error state around the async provider resolution: the Promise.all has no .catch and the render (144-183) has no empty/loading/error branch, so a failed or empty search silently renders empty suggestion containers. Partial because the wiring works mechanically but does not meet the product-owner expectation of real org data/permissions.
- **Fix:** Inject providers, the policy engine, and MentionContext (callerId, locale, timezone, now) via props/context sourced from a real directory + RBAC backend instead of constructing fixtures inside the component; derive `now` from Date.now() and keep the fixed value only as a test seam. Add empty-state ('No people or groups found'), loading, and error UI around the async resolution, and add a .catch on the Promise.all so rejected provider promises surface to the user instead of becoming an unhandled rejection / silent empty render.
- **Verified:** CONFIRMED by direct read. Context constants incl. now '2026-05-16T00:00:00.000Z' at lines 21-26; hardcoded policy (revocations isRevoked: async()=>false, visibility canSee denying refId containing 'secret') and canned user/group search fixtures at lines 53-84; render block 144-183 has no loading/empty/error branch and the Promise.all().then() at 103-111 has no .catch(). One scoping correction the original finding got right: the fixed `now` only affects date parsing — RBAC decisions use `new Date().toISOString()` inside visibleOrHidden (@lash/mentions index.ts:40), not ctx.now — so the date-relative claim is correctly limited to natural-date parsing. issue_type 'ux' and severity 'medium' are appropriate (fixtures + missing states are UX-facing but this is an acknowledged scaffold, not a hard break). suggested_fix is sound; I added the missing .catch detail since the absent rejection handler is a real (not merely silent) gap.

### [ ] F-C10-03 · Timezone-aware ISO output `bug`

- **Cluster:** C10 · **Verdict:** partial
- **Problem:** timezoneOffset (packages/mentions/src/index.ts:87-91) only handles two hard-coded zones: 'Asia/Tokyo' -> '+09:00' and 'UTC' -> 'Z'; EVERY other IANA zone falls through to the final `return 'Z'`. The ISO is assembled at line 119 as `<date>T<HH>:<MM>:00<offset>` using that offset, while the display at lines 120-127 formats `new Date(iso)` with the REAL timeZone: ctx.timezone. For any unsupported zone (e.g. America/New_York), the wall-clock hour the user typed is stamped with a UTC 'Z' offset, so the stored absolute instant is wrong AND the display re-renders that instant in the real zone, yielding a label that disagrees with the typed time. This breaks the story goal that the time 'means the same thing to all collaborators'.
- **Fix:** Compute the real UTC offset for ctx.timezone (e.g. Intl.DateTimeFormat('en-US',{timeZone:ctx.timezone,timeZoneName:'longOffset'}) and extract the GMT offset for the resolved date, or use a date library / build the ISO from a true zoned instant) so the offset always matches the timeZone used for display. Add a unit test for an arbitrary IANA zone such as America/New_York asserting offset/display agreement, since current tests (mention-date-parse.test.ts, mention-date-locale.test.ts) only cover Asia/Tokyo.
- **Verified:** CONFIRMED. Code at lines 87-91 has exactly the described 2-entry fallback; the bare `return 'Z'` catches all other zones. ISO uses timezoneOffset(ctx.timezone) at line 119, display uses real timeZone:ctx.timezone at 126. Worked the America/New_York example: a user typing 'next Friday 3pm' gets iso '...T15:00:00Z', and the display renders that as 11:00 AM EDT -- so both the absolute instant (15:00Z = 11am NY, not the intended 3pm NY) and the label diverge from intent. Genuine correctness bug, not cosmetic. Verified test gap: both test files in packages/testing/unit/mentions/ only use Asia/Tokyo, so non-Tokyo/UTC zones are untested. Severity medium is appropriate -- it is a real cross-collaborator time mismatch, but only for zones outside the two hard-coded ones and the demo wiring currently pins Asia/Tokyo, limiting current blast radius. issue_type 'bug' is correct. Suggested fix is sound; I tightened it to name a concrete Intl approach and the specific test to add.

### [ ] F-C11-08 · Inline suggestion marks (inserted/deleted) in editor schema `logistical`

- **Cluster:** C11 · **Verdict:** fail
- **Problem:** schema/suggest.ts documents 'inserted'/'deleted' marks and an Accept/Reject command set gated by LashSuggestOptions.enabled, but buildSuggestExtensions() unconditionally returns an empty Extensions array (`export const buildSuggestExtensions = (_options?: LashSuggestOptions): Extensions => [];`, suggest.ts:20). It is spread into createLashEditorExtensions (schema/index.ts:37) but contributes nothing. No suggestion marks, commands, or inline track-changes visuals are registered with TipTap. The classic inline track-changes UX described in the story does not exist; shipped suggest mode operates entirely at the history/version layer (intent='suggest'). The CSV already labels this status='stub'.
- **Fix:** Implement buildSuggestExtensions to register 'inserted'/'deleted' marks with author/ts attributes plus hover surfacing and an Accept/Reject command set gated by options.enabled; OR, if the history-layer approach is the intended ship, remove/repurpose schema/suggest.ts and re-scope the story to match the history-based design rather than inline marks.
- **Verified:** CONFIRMED independently. Read suggest.ts:1-21 in full: header explicitly says 'Status: SLOT — to be filled by M4/E4' and line 20 returns `[]` unconditionally regardless of options.enabled. schema/index.ts:17,37 confirms it is imported and spread but contributes nothing. STORIES.csv line 87 itself marks status='stub'. Inline marks/commands are genuinely absent. verdict=fail, issue_type=logistical, severity=medium all appropriate; suggested_fix offers both the implement and re-scope paths, which is sound.

### [ ] F-C16-04 · Share/access audit logging `logistical`

- **Cluster:** C16 · **Verdict:** partial
- **Problem:** Two gaps: (1) the revoked branch of validate() (lines 131-133) returns without recording any audit event, unlike the invalid (111-117) and expired (122-129) branches, so a revoked-token validation attempt leaves no audit trail for the most security-relevant case; (2) createAuditLog accepts an adapter:'memory'|'postgres' option but ignores it entirely (the param is `_config`, line 201), so audit is always in-memory and lost on reload despite the API implying durable storage.
- **Fix:** Add a config.audit.record call in the revoked branch of validate() (e.g. action:'share-link.revoked' or 'share-link.access' with reason:'revoked') so revoked attempts are logged consistently with invalid/expired. Either implement the postgres adapter or remove/throw on the unused option so the API does not imply durability it lacks.
- **Verified:** CONFIRMED by code. packages/share/src/index.ts:131-133 — revoked branch `if (await config.revocations.isRevoked(stored.jti)) { return { ok: false, reason: 'revoked' }; }` records nothing, in direct contrast to the invalid branch (111-117) and expired branch (122-129) which both call config.audit.record. Line 201 `createAuditLog = (_config: { adapter: 'memory' | 'postgres' })` and 202-215 (in-memory array, adapter never read) confirm the postgres option is dead. Severity medium is justified: the missing-revoked-audit is a genuine audit-trail hole on the highest-value security event, and the phantom postgres adapter misrepresents durability. issue_type logistical is acceptable, though one could argue the missing revoked audit borders on a spec/bug issue since the story (F-C16-04) explicitly promises a trail for revocations; logistical/partial is a fair characterization. Fix is sound.

### [ ] F-C16-06 · History/diff & chat redaction by scope `logistical`

- **Cluster:** C16 · **Verdict:** partial
- **Problem:** redactDiff blanket-replaces the text of every 'inserted'/'deleted' span with '[redacted]' whenever policy.rules is non-empty, and returns the diff untouched when rules is empty. The policy.rules path/action fields are never read (only rules.length is checked as a boolean toggle), callerId (\_callerId) is unused, and PolicyStore.resolve is never consulted by redactDiff. So the 'omit'/'hash' actions and per-path targeting in the RedactionPolicy type are dead configuration; only span-kind blanket text redaction is supported.
- **Fix:** Honor policy.rules: match each rule's path/action against spans and apply redact/omit/hash accordingly, and resolve the active policy via PolicyStore.resolve(policySha, version) rather than treating rules as a non-empty/empty flag. Until then, document that only blanket span-text redaction (keyed on span kind) is supported.
- **Verified:** CONFIRMED by code. packages/share/src/index.ts:162-177 — redactDiff: line 167 `if (!policy.rules.length) return diff;` is the only use of rules (boolean toggle); lines 170-175 map spans replacing text keyed solely on span.kind ('inserted'/'deleted'); rule.path and rule.action are never accessed; the third arg is `_callerId` (unused). PolicyStore.resolve exists (21-23, 232-236) but is not invoked from redactDiff. SharePanel.tsx:110-113 confirms the edit-scope empty-rules vs full-policy toggle and N-of-M display. Severity medium is reasonable given it is demo-correct but the typed policy contract (omit/hash/per-path) is unimplemented. issue_type logistical is fine. Fix is sound.

### [ ] F-C17-03 · Chat transcript redaction indicator for restricted viewers `ux`

- **Cluster:** C17 · **Verdict:** partial
- **Problem:** The chat-redaction indicator in SharePanel is purely presentational: SharePanel.tsx:281-283 flips text between 'chat visible' and 'chat transcript redacted' based solely on the local `scope` state, but nothing in doc-chat filters or masks ChatMessage bodies. ThreadStore.list() (packages/doc-chat/src/index.ts:197) maps each message to a full shallow copy `({ ...message })` including the complete `body`; reply()/create() likewise return full message objects. There is no decide()/capability check anywhere in doc-chat. The label also keys off `scope` (set only by issueLink/createInvite) rather than `effectiveScope` (which honors the `accessScope` prop), so a viewer arriving via accessScope without issuing a link always sees the default 'chat transcript redacted'.
- **Fix:** Either implement real transcript redaction in doc-chat (filter/mask message bodies via a policy/capability check when scope!='edit'), or relabel the indicator as an explicit status badge so it does not assert protection the system does not enforce. At minimum, key the label off `effectiveScope` rather than `scope` for consistency with the rest of the panel.
- **Verified:** Independently reproduced from code. SharePanel.tsx:281-283 confirmed (label keyed off `scope`; `effectiveScope` is computed at line 147 but unused by this label). doc-chat list() at line 180-199 returns full bodies with no redaction/RBAC; no decide() call exists in doc-chat (grep confirms the only decide() caller in the repo is packages/mentions/src/index.ts:38). issue_type=ux and severity=medium are appropriate. Important context that tempers but does not refute the finding: the STORIES.csv acceptance criteria for F-C17-03 itself describes this as 'a presentational gate only ... there is no function that actually filters/masks ChatMessage bodies', and the story is marked partial/story-drafted. So the cosmetic behavior matches the drafted AC; the genuine defect is the gap between the user-facing story goal ('discussion content is hidden from me') and an implementation that hides nothing — a silent-protection mismatch. Suggested fix is sound; I added the effectiveScope correction.

### [ ] F-C17-05 · Policy decision engine with redacted refusal reason `logistical`

- **Cluster:** C17 · **Verdict:** partial
- **Problem:** createPolicyEngine().decide() is implemented per the frozen contract (packages/rbac/src/index.ts:63-97): anonymous -> {allow:false,reason:'no-access'}; share-link -> 'expired' when token.expiresAt<=ctx.now, 'revoked' via revocations.isRevoked(jti), else allow iff capabilitiesForScope(token.scope) includes cap ('scope-mismatch' otherwise); user on mention/chip -> allow or {allow:false,reason:'redacted'} via visibility.canSee; user on doc -> allow. The gap is integration scope: the package header (lines 1-13) declares it a SCAFFOLD/frozen contract, and the only invoker in the codebase is packages/mentions/src/index.ts:38. Neither the history view nor the chat view routes through decide(); history redaction is done ad hoc via redactDiff and chat 'redaction' is the cosmetic label from F-C17-03. The 'redacted' refusal path and uniform enforcement are therefore not wired into history/chat.
- **Fix:** Route history-read/restore and chat-read decisions through createPolicyEngine().decide() so the 'redacted' reason and scope checks are enforced uniformly, OR explicitly document that decide() is intentionally mentions-only for this milestone (the SCAFFOLD header already gestures at this but should state the scope boundary so the non-integration is not mistaken for a defect).
- **Verified:** Independently reproduced. decide() impl at rbac/src/index.ts:63-97 matches the described contract; SCAFFOLD/frozen header confirmed at lines 1-13. repo-wide grep for '.decide(' returns exactly one caller (mentions/src/index.ts:38), confirming history and chat do not invoke it. The STORIES.csv AC for F-C17-05 itself states 'The engine is declared a SCAFFOLD/frozen contract; it is not invoked by the history or chat views (only mentions calls decide)', so this is a known, drafted milestone boundary rather than a regression. issue_type=logistical and severity=medium are appropriate — this is an integration-scope/architectural-consistency gap, not a logic bug. Suggested fix is sound; the documentation alternative is the more accurate characterization given the explicit SCAFFOLD status.

### [ ] F-C18-05 · AI chat answer with doc citation + jump `bug`

- **Cluster:** C18 · **Verdict:** partial
- **Problem:** askAi requires a non-empty selection, sets a grounded chat answer and a doc citation {type:'doc', baseVersion, rangeFrom, rangeTo}, the citation button shows the cited slice, and jumpToCitation focuses the editor and runs setTextSelection to navigate, recording jumpTarget for an sr-only confirmation. The grounding offsets are derived from currentText.indexOf(selectionText) (plaintext), then jumpToCitation hardcodes a +1 ProseMirror offset (rangeFrom+1 / rangeTo+1). The +1 assumes exactly one leading structural token before the text, which only holds for the simplest single-paragraph doc; for multi-block docs the indexOf offset (flat text with '\n' joins) does not map to ProseMirror positions by a constant +1, so the jump lands on the wrong range. indexOf also resolves to the FIRST occurrence, mis-citing repeated text.
- **Fix:** Capture the actual ProseMirror selection positions (editor.state.selection.from/to) at request time instead of reconstructing offsets via indexOf, and store those PM positions on the citation so jumpToCitation can setTextSelection directly without a fragile +1 fudge.
- **Verified:** CONFIRMED. AIPanel.tsx:90-95 derives selectionRange from currentText.indexOf(selectionText) (first-occurrence flat-plaintext offset, joined with '\n' per selectedText at line 51 / textBetween). askAi (204-217) stores those plaintext offsets as the doc citation rangeFrom/rangeTo. jumpToCitation (219-225) then maps them to PM positions via a hardcoded +1 (from = rangeFrom+1, to = max(from, rangeTo+1)). Both flaws are real: (1) a constant +1 only corrects for a single leading open-token; multi-block docs accumulate one position per block boundary (textBetween '\n' joins collapse those to 1 char), so the PM mapping drifts for any doc past the first block; (2) indexOf returns the first match, so repeated selected text mis-locates. Note editor.state.selection.from/to (the true PM positions) ARE already available at request time (used by selectedText at line 49) but are discarded in favor of the reconstructed offsets, making the suggested fix straightforward and correct. Severity 'medium' and issue_type 'bug' are right -- the citation/jump feature actively navigates to the wrong range in realistic docs, though it degrades rather than crashes.

### [ ] F-C22-09 · RTL / bidirectional text support (i18n direction) `a11y`

- **Cluster:** C22 · **Verdict:** fail
- **Problem:** No RTL/bidi or i18n direction support exists. apps/web/app/layout.tsx:36 hardcodes <html lang="en"> with no dir attribute. No editor node, container, or CSS emits dir=/direction:/unicode-bidi (grep finds only flex-direction). There is no locale/i18n layer; the only locale references are String.prototype.localeCompare calls used for sorting, not internationalization. Arabic/Hebrew content renders left-to-right, breaking bidirectional display. The story's expected_behaviour documents this absence and marks impl_status='missing', making this fail-by-design.
- **Fix:** Add per-block/per-document direction: emit dir="auto" on paragraph/heading block nodes so bidi text resolves per the Unicode Bidirectional Algorithm, apply CSS unicode-bidi: isolate on those nodes, and drive <html dir> from a locale setting (auto/ltr/rtl). Minimum viable: dir="auto" on block-level editor nodes plus container CSS.
- **Verified:** Independently reproduced from code. layout.tsx:36 confirmed: lang="en" with no dir. Repo-wide grep across packages/editor-core/src, apps/web/app, apps/web/components shows no genuine RTL/bidi/dir/locale handling -- matches are only flex-direction (CSS layout), localeCompare (string sorting), and convertList\* (markdown list conversion), none of which provide i18n direction. apps/web/e2e/a11y contains no dir/RTL spec. FEATURE_AUDIT/STORIES.csv row F-C22-09 explicitly records impl_status='missing' and the same evidence, confirming fail-by-design. CONFIRMED. issue_type a11y is correct (bidi rendering is a WCAG/i18n accessibility concern). Severity: keeping medium -- it fully blocks RTL-language authoring -- but note this is an acknowledged scope gap on an English-only product (a deliberate non-feature) rather than a regression in a shipped feature, so low is also defensible. The suggested_fix is technically sound: dir="auto" + unicode-bidi: isolate correctly applies the Unicode Bidi Algorithm at block level; no correction needed.

### [ ] F-C24-05 · Canonical-hash storage policy `logistical`

- **Cluster:** C24 · **Verdict:** fail
- **Problem:** The header comment states that parentSha/resultSha hashing MUST go through @lash/types/hashCanonical and storage must never invent its own hash. This is documented only as a doc comment: resultSha/parentSha are accepted as plain string parameters and there is no import of hashCanonical, no hashing call, and no validation that supplied shas are canonical. Since every factory is a stub, the policy is unenforced. Note hashCanonical does exist (packages/types/src/index.ts:517) so the policy is implementable but simply unwired.
- **Fix:** When the stores are implemented, import hashCanonical from @lash/types and either compute resultSha via hashCanonical(doc) at save/append time or validate that a caller-supplied sha equals hashCanonical(doc), rejecting mismatches. Until then the policy is non-binding text.
- **Verified:** CONFIRMED. index.ts:1-7 states the policy in a doc comment; the only import is 'import type { DocumentId, HistoryEntry }' (line 9) — no hashCanonical import or call anywhere in the file. hashCanonical is genuinely defined at packages/types/src/index.ts:517, so the policy is implementable but unwired. STORIES.csv F-C24-05 tagged 'stub'. Severity medium (vs high for the others) is appropriate: this is a policy-enforcement gap that only matters once the stores exist, not a missing user-facing capability. Suggested fix is sound, including the honest caveat that it is non-binding text until the stores ship.

### [ ] F-C25-07 · Sidebar workspace navigation (brand + nav links) `ux`

- **Cluster:** C25 · **Verdict:** partial
- **Problem:** Sidebar renders the 'Lash' brand plus PRIMARY_NAV (Current doc [active, aria-current=page], Home, Inbox, Starred, Shared with me) and SECONDARY_NAV (All documents, Settings). Each NavItem is a <button> with icon+label+title but NO onClick handler — they are non-functional presentational placeholders. They look like real navigation but do nothing when clicked, which is a UX trap (dead controls presented as live). The story's expected_behaviour explicitly acknowledges this stub state, so it matches the code, but it remains a real UX gap for end users.
- **Fix:** Either wire the nav items to routes/actions (Home, All documents, Settings, etc.) or visually mark them as disabled/coming-soon (aria-disabled, muted styling) so users aren't misled into clicking dead buttons.
- **Verified:** CONFIRMED via code. Sidebar.tsx:31-42 defines static PRIMARY_NAV/SECONDARY_NAV; NavItem (44-57) renders a <button> with icon/label/title and NO onClick, rendered at 79-81 and 106-108 without handlers. The 'Current doc' item carries aria-current='page' yet does nothing, which compounds the trap (it implies live navigation state). STORIES.csv:188 marks status='stub' and the expected_behaviour openly says these are 'presentational placeholders that do not navigate anywhere' — so code matches the documented spec, but it is still a genuine UX gap (dead controls). Medium severity is justified: multiple visible top-level controls are non-functional. Suggested_fix is sound.

### [ ] F-C25-11 · TopBar document chrome `ux`

- **Cluster:** C25 · **Verdict:** partial
- **Problem:** TopBar shows a document icon + read-only title text (topbar-doc-title), AutosaveIndicator, focus/suggest toggles (aria-pressed, swapping icon/label), an AvatarStack, and a Share button (aria-expanded/aria-controls=lash-rail) opening the share rail. The collaborator stack is HARD-CODED demo data (Apollo/Ada Lovelace/Grace Hopper, all 'viewing now'), shown as if live presence. Real presence exists elsewhere (realtimeSnapshot.peers in EditorWorkspace), so the static avatars are misleading fake data.
- **Fix:** Drive the AvatarStack from realtimeSnapshot.peers (real presence) instead of the static PRESENT_COLLABORATORS array, or label/remove it as a placeholder until presence is wired into the TopBar.
- **Verified:** CONFIRMED via code. TopBar.tsx:27-31 hard-codes PRESENT_COLLABORATORS (Apollo/Ada Lovelace/Grace Hopper); 117-126 renders them in the AvatarStack with title '<name> · viewing now', implying live presence. By contrast EditorWorkspace.tsx:1421 derives remotePeers = realtimeSnapshot.peers and renders real presence in a separate presence bar (1455-1463) plus cursor markers (1493-1505). So genuine presence data exists but the TopBar avatars ignore it and show fixed fakes. STORIES.csv:192 admits 'The collaborator stack is static demo data, not live presence,' so code matches the documented spec but the UX is misleading. Medium severity appropriate. Suggested_fix is sound.

## LOW severity (24)

### [ ] F-C02-06 · Toolbar active-state highlighting and accessibility `a11y`

- **Cluster:** C02 · **Verdict:** partial
- **Problem:** Active-state mapping is correct in isToolbarButtonActive (toolbar.ts:34-63): each id maps to the right isActive() check and insert-table is always false. The live toolbar (panels/EditorToolbar.tsx:80-95) renders bare <button class='lash-icon-btn'> with data-active and aria-pressed, NOT the ToolbarButton.tsx component, so the inline activeStyles in ToolbarButton.tsx:23-26 is dead code; the active highlight depends entirely on a CSS rule. That CSS rule DOES exist (globals.css:836-840: .lash-icon-btn[data-active='true'],[aria-pressed='true'] sets coral background/color), so the highlight is functional — it is just driven by CSS, not by ToolbarButton.tsx. Secondary a11y issue: aria-pressed='false' is set on the insert-table action button (EditorToolbar.tsx:90), which is not a toggle, so a screen reader announces it as an unpressed toggle, which is semantically inaccurate.
- **Fix:** No CSS fix needed — the active highlight rule already exists at globals.css:836-840. Optionally remove the unused ToolbarButton.tsx activeStyles (dead code) or reuse the component to dedupe. For the a11y issue, omit aria-pressed on the insert-table button (and any non-toggle action) since it is an action, not a toggle.
- **Verified:** CONFIRMED with one correction. Every code claim checks out: EditorToolbar.tsx:80-95 uses bare lash-icon-btn buttons and does not import ToolbarButton.tsx (verified no import); ToolbarButton.tsx activeStyles is indeed unused by the live toolbar. The aria-pressed='false' on the insert-table action (line 90, always-false from toolbar.ts:58-59) is a genuine low-severity a11y inaccuracy. CORRECTION to suggested_fix: the finding said to 'Confirm a CSS rule exists' — I verified it DOES exist at globals.css:836-840, so the active highlight is NOT broken; only the ToolbarButton.tsx dead code and the aria-pressed-on-action issue remain. Severity low and issue_type a11y are appropriate; the highlight itself is not actually broken so this stays partial, not fail.

### [ ] F-C04-04 · Markdown export (serializeDocToMarkdown) `bug`

- **Cluster:** C04 · **Verdict:** partial
- **Problem:** In serializeDocToMarkdown, convertTableToMdast extracts each table cell's inline content via cell.content?.[0]?.content (index 0 only), so it reads only the inline children of the FIRST block of a cell. If a table cell contains multiple paragraphs or a non-paragraph block (a user can create a second paragraph in a cell with Enter), the additional blocks are silently dropped with no warning, causing data loss on export. Code-block export is also unhandled (no codeBlock case in convertDocNodeToMdast, lines 472-516; unsupported nodes fall to the default branch and emit an empty paragraph + 'Unsupported node' warning), but code blocks are not in the schema so that part is genuinely minor.
- **Fix:** In convertTableToMdast, flatten/concatenate the inline content across ALL of cell.content blocks (e.g. flatMap each child block's content through convertInlineForExport, optionally joining paragraphs with a break) instead of only cell.content[0].content. Since mdast tableCell children must be PhrasingContent (inline), do not nest block nodes; concatenate inline runs. Additionally emit a warning when a cell contains more than one block, because GFM tables cannot represent multi-block cells, so the user is informed of the lossy conversion.
- **Verified:** CONFIRMED by reading packages/editor-core/src/markdown/index.ts. Line 576 (within convertTableToMdast 568-597) reads only (cell.content?.[0]?.content) -- the inline children of the first block of each cell -- so any second paragraph or non-paragraph block in a cell is silently dropped without adding to the warnings Set. The underline-as-emphasis warning (line 403, applyMarks) and the clamped 1-3 heading depths (line 479) are accurately described. The code-block gap is also real (no codeBlock case in the switch at 472-516) but correctly downgraded to minor since codeBlock is not in the schema. Severity 'low' and issue_type 'bug' are appropriate: the lossy path is reachable by normal user interaction (multi-paragraph cells) but uncommon and degrades gracefully (first paragraph survives), and the 'partial' verdict is correct because core table/inline/list export works. Corrected the suggested_fix to clarify that mdast tableCell children must be inline (PhrasingContent), so the fix should concatenate inline content from all blocks rather than push block nodes, and should add a warning for the truly lossy multi-block case.

### [ ] F-C05-08 · Large-table virtualization `logistical`

- **Cluster:** C05 · **Verdict:** fail
- **Problem:** No virtualization exists. @lash/tables-media defines TableViewport/TableVirtualizer interfaces and a createTableVirtualizer factory that immediately throws 'createTableVirtualizer: not implemented (M5/F4)'. The file is marked SCAFFOLD and nothing in the editor consumes it; companion createUploadPipeline also throws. Off-screen rows/columns are not virtualized at all.
- **Fix:** Either implement createTableVirtualizer (row/col windowing driven by viewport + TableMap) and wire it into the table node view, or de-scope the story until M5/F4; do not represent it as delivered. The CSV already correctly marks impl_status='stub'.
- **Verified:** CONFIRMED by code. packages/tables-media/src/index.ts:1-9 is explicitly marked 'Status: SCAFFOLD — implement in M5/F4'. Lines 25-30: createTableVirtualizer throws new Error('createTableVirtualizer: not implemented (M5/F4)'); lines 37-39: createUploadPipeline likewise throws. A repo-wide grep for createTableVirtualizer / @lash/tables-media / TableVirtualizer across packages and apps (excluding the source file) returned ZERO consumers — nothing imports or wires it. So no off-screen virtualization exists. STORIES.csv:42 already records impl_status='stub', so the audit is internally consistent. Severity low is reasonable since the story is honestly marked stub (not falsely claimed as delivered) and large-table perf is a non-blocking enhancement; issue_type logistical fits (scope/delivery gap, not a runtime defect). Suggested fix is sound.

### [ ] F-C06-03 · Async upload pipeline with progress and placeholder swap `ux`

- **Cluster:** C06 · **Verdict:** partial
- **Problem:** Progress wiring and placeholder swap are correctly implemented: onProgress dispatches setNodeMarkup of the 'progress' attr (rendered 'Uploading… N%' with N=round(progress\*100)); on resolve attrs are set src=result.src, status='idle', uploadId=null, progress=1, previewSrc=null (img.src swaps from preview to uploaded src); width is taken from result.width only when the user has not manually resized (currentWidth undefined or still === initialWidth); preview object-URL is revoked and the entry/disposer removed on success. The only gap is that the production uploader (createBrowserImageUploader) is a hardcoded mock that ticks fake progress on a timer and resolves to a synthetic assets.lash.dev URL — no real upload occurs. The spec itself acknowledges this is a mock, so it is spec-accurate, but ships fake media URLs as if real.
- **Fix:** Replace createBrowserImageUploader's mock with a real upload implementation (POST file to an asset endpoint, stream real progress via XHR/fetch upload events) before production; gate the mock behind a test-only flag so production never serves fabricated assets.lash.dev URLs.
- **Verified:** CONFIRMED. Editor wiring at image.ts:97-156 and updateDom at 576-616 match the description exactly: onProgress dispatch (101-104), success attrs (113-119), width guard (124-131), revokePreview+delete on success (187-191), progress label Math.round((attrs.progress??0)\*100) at 591-592, src swap preview->src at 587-609. createBrowserImageUploader.ts:13-66 is a mock with setInterval tick to 0.95 (lines 37-42) and setTimeout resolving to `https://assets.lash.dev/mock/image-${uploadCounter}.png` (lines 31-32, 58-62). It also reads a window.\_\_lashImageUploadMock test hook (lines 17-24). The LashImageUploader interface is injectable, so swapping in a real uploader is straightforward. Severity low and issue_type ux are appropriate — this is a known/spec-acknowledged mock, not a runtime defect. Suggested fix is sound.

### [ ] F-C06-06 · Alt text editing `a11y`

- **Cluster:** C06 · **Verdict:** partial
- **Problem:** The 'Alt text' button (data-testid=image-alt-edit) opens window.prompt seeded with the current alt; on non-null confirm it updates the alt attr via setNodeMarkup; img.alt is set and the info label shows 'Alt: <text>' or 'Alt text missing' when empty. Functionally correct per spec. The accessibility gaps: the alt-editing affordance is a native window.prompt (not an accessible in-editor dialog, blocks the main thread, and is unstyled), and the slider has no label and the figure has no role/labelling for the image region.
- **Fix:** Replace window.prompt with an inline accessible text input/dialog (role=dialog, focus trap, labeled input) so alt editing is keyboard- and screen-reader-friendly and non-blocking; add aria-label to the width slider and an accessible name to the figure.
- **Verified:** CONFIRMED. image.ts:481-488 uses window.prompt('Image description (alt text)', this.node.attrs.alt ?? '') with a null guard, then updateNodeAttrs({alt}). img.alt set and baseLabel logic at 612-614. The width slider (491-519) has no aria-label and no associated <label> element; the retry/alt buttons do have visible text content (acceptable), so the original note's claim that edit/retry buttons 'lack aria-labels' is technically true but not a real defect since visible text provides the accessible name — I trimmed that from the description. The figure element (437) has data-testid but no role or aria-label. The window.prompt and unlabeled-slider a11y gaps are genuine. Severity low and issue_type a11y are correct. Suggested fix is sound.

### [ ] F-C06-08 · Markdown image import/export roundtrip `spec-error`

- **Cluster:** C06 · **Verdict:** partial
- **Problem:** Import correctly converts remark 'image' and 'imageReference' nodes to TipTap image nodes with {src, alt}; imageReference resolves src from collected link definitions and warns on a missing definition; images inside headings are downgraded to alt text with a warning. Export emits reference-style images via convertImageToMdast (identifier 'image-<n>', reuses an existing definition for the same src) plus appended 'definition' blocks. Two spec deviations: (1) The spec says 'Images without src are dropped', but this only holds inside paragraph conversion (convertParagraphToMdast checks child.attrs?.src at line 432) — a TOP-LEVEL block image node with empty src is NOT dropped: convertDocNodeToMdast's 'image' case (lines 490-496) unconditionally calls convertImageToMdast, emitting an empty-url reference and a '[image-n]: ' definition. Since the image node is group='block', top-level is the primary path for block images, so src-less images survive export. (2) The import heading-fallback for an imageReference uses definitions.get(identifier)?.title rather than node.alt, so reference images inside headings export their (often absent) definition title, not their alt.
- **Fix:** In convertDocNodeToMdast's top-level 'image' case (and convertInlineForExport, which is also unconditional), skip the node when node.attrs?.src is empty so block-level src-less images are dropped consistently with paragraph handling; if the heading downgrade should use alt for references, change the import fallback at line 114 to prefer node.alt before definition.title.
- **Verified:** CONFIRMED, with a minor scope correction. Deviation (1) verified: convertParagraphToMdast guards on child.attrs?.src (markdown/index.ts:432), but convertDocNodeToMdast's 'image' case (490-496) calls convertImageToMdast unconditionally. The image node is group:'block' (image.ts:621), so top-level block images are the real-world path and they bypass the src check — confirmed src-less images are NOT dropped at block level. I also noted convertInlineForExport (518-528) is likewise unconditional (table-cell path), so the fix should cover it too; I added that to the suggested_fix. Deviation (2) verified: import-path line 114 uses definitions.get(node.identifier)?.title for imageReference fallback inside headings while node.alt is available — minor inconsistency. Severity low and issue_type spec-error are appropriate (no crash, cosmetic/edge-case roundtrip fidelity). Suggested fix is sound.

### [ ] F-C06-09 · tables-media virtualization & transform pipeline `logistical`

- **Cluster:** C06 · **Verdict:** fail
- **Problem:** The @lash/tables-media package only declares the TableVirtualizer and UploadPipeline (upload + resize transform) interfaces; both factory functions createTableVirtualizer and createUploadPipeline throw 'not implemented' errors. The file header explicitly marks it SCAFFOLD for M5/F4. No real virtualization or server-side image transform exists. This is a developer-facing story and is openly scaffolded, so severity is low, but as written the feature is absent.
- **Fix:** Implement createTableVirtualizer (windowed visible-cell selection over TableViewport) and createUploadPipeline (real upload + server-side resize transform against the configured endpoint), or de-scope the story until M5 and mark it explicitly deferred so it is not counted as delivered.
- **Verified:** CONFIRMED. tables-media/src/index.ts:1-9 header states 'Status: SCAFFOLD — implement in M5/F4'. createTableVirtualizer (25-30) throws 'createTableVirtualizer: not implemented (M5/F4)'; createUploadPipeline (37-39) throws 'createUploadPipeline: not implemented (M5)'. Only interfaces (TableViewport, TableVirtualizer, UploadPipeline) are declared. STORIES.csv row 51 itself marks this story status 'stub'. The feature is genuinely absent. 'fail' verdict and 'logistical' issue_type are correct; severity low is fair since this is an explicitly-deferred developer-facing scaffold rather than a broken user-facing feature. Suggested fix (implement or formally de-scope/mark deferred so it isn't counted as delivered) is sound.

### [ ] F-C09-02 · Mention extension options not wired to suggestion plugin `logistical`

- **Cluster:** C09 · **Verdict:** fail
- **Problem:** LashMentionOptions declares `context` and user/group `providers`, and the option's inline comment promises 'the suggestion plugin queries this' (mentions.ts:11-15), but buildMentionExtensions ignores its argument (`_options`) and returns only the bare LashMention node (mentions.ts:73). No TipTap @tiptap/suggestion plugin is registered. createLashEditorExtensions passes options.mentions into buildMentionExtensions (schema/index.ts:35) where it is silently dropped. In-editor suggestion is delivered entirely by the separate React MentionPanel, so the documented editor-level provider wiring is dead/unreachable config. This matches the spec, which explicitly classifies F-C09-02 as a 'stub', so users still get suggestions via the panel; severity is low because the gap is dead config, not a user-facing break.
- **Fix:** Either remove the unused providers/context fields from LashMentionOptions and the misleading 'the suggestion plugin queries this' comment so the API does not imply capability that does not exist, or implement a @tiptap/suggestion plugin inside buildMentionExtensions that queries the passed providers so suggestion logic is not duplicated in the React panel.
- **Verified:** CONFIRMED by direct read. mentions.ts:73 is verbatim `export const buildMentionExtensions = (_options?: LashMentionOptions): Extensions => [LashMention];` — the param is prefixed `_` and never referenced. Options interface (lines 11-15) declares context+providers with the cited comment. schema/index.ts:35 confirms the call passes options.mentions in. No Suggestion import anywhere in mentions.ts. The spec row F-C09-08... (and F-C09-02 status column) marks this story 'stub', so the description is accurate and severity low is correct. issue_type 'logistical' fits (dead/misleading config, not a runtime defect). suggested_fix is sound as written.

### [ ] F-C10-06 · Date suggestion wiring in the mention panel `ux`

- **Cluster:** C10 · **Verdict:** partial
- **Problem:** MentionPanel.tsx matches a trailing @<query> via /@([^@\n]{1,40})$/ (line 87), trims it (line 95), calls parseDateMention (line 96), and on a visible date sets that single suggestion and clears anonymized results, short-circuiting before user/group lookups (lines 97-101). The button has onClick -> insertMention (line 154) and insertMention is correctly wired (lines 118-142). The wiring itself is correct. The real gap: the MentionContext passed to parseDateMention and the providers is a module-level constant (lines 21-26) hardcoded with now:'2026-05-16T00:00:00.000Z' and timezone:'Asia/Tokyo'. In production this resolves 'next Friday' relative to a frozen fake May 2026 date in Tokyo time, so real users get a wrong date and zone. Demo/test scaffolding is leaking into the shipped panel.
- **Fix:** Remove the hardcoded `now` (omit it so parseDateMention falls back to new Date()) and derive timezone/locale from the browser at runtime via Intl.DateTimeFormat().resolvedOptions().timeZone and navigator.language (with safe fallbacks), instead of the fixed now/Asia/Tokyo constant, so date suggestions reflect the real current date and the user's actual zone. Compute the context inside the component (e.g. a useMemo) rather than as a module constant.
- **Verified:** CONFIRMED. The hardcoded context object is at lines 21-26 exactly as described (now:'2026-05-16T00:00:00.000Z', timezone:'Asia/Tokyo', locale:'en-US') and is passed verbatim to parseDateMention at line 96 and to both providers at lines 104-105 -- it is a shipped 'use client' component, not a test file, so the scaffolding does leak into production. Wiring claims verified: regex line 87, trim line 95, short-circuit 97-101, button onClick line 154, insertMention 118-142 including iso passthrough at 136. The frozen `now` would make every relative date resolve against May 16 2026 regardless of the actual date -- a real correctness/UX defect once the panel is exercised by real users. issue_type 'ux' is defensible but this is arguably closer to a 'bug' (wrong date is a correctness problem, not just presentation); I left it as 'ux' to respect the original framing but note 'bug' would also be valid. Severity 'low' is reasonable given this appears to be early demo wiring with mock user/group search data (lines 65-81) -- the whole panel is still scaffold-grade, so the hardcoded context is consistent with that and not yet user-facing in a finished sense. Suggested fix is sound; I added that the context should be computed in-component (useMemo) rather than a module constant.

### [ ] F-C11-05 · Reject a suggestion `ux`

- **Cluster:** C11 · **Verdict:** partial
- **Problem:** Reject works correctly: handleRejectSuggestion (EditorWorkspace.tsx:1074-1109) loads the parent revision text, appends a replace_text op restoring it (intent 'edit') guarded by `currentText === targetText`, applies it via setContent under applyingHistoryRef, advances the head with commitHistoryHead, records a 'rejected' resolution, and refreshes versions/blame. However the Accept/Reject action buttons (HistoryPanel.tsx:293) are gated only on `selectedEntry.intent === 'suggest' && !selectedSuggestionAccepted`, with no check for an existing 'rejected' resolution. The status line (HistoryPanel.tsx:273) DOES account for `selectedSuggestionResolution?.action === 'rejected'`. Since reject records a resolution but never touches acceptedSuggestionIds, selectedSuggestionAccepted stays false; re-selecting the original suggest entry re-shows the Accept/Reject buttons while the status reads 'Rejected suggestion'. Status and action gate are inconsistent.
- **Fix:** Add the rejected-resolution check to the action-button gate at HistoryPanel.tsx:293, e.g. `selectedEntry.intent === 'suggest' && !selectedSuggestionAccepted && selectedSuggestionResolution?.action !== 'rejected'`, so resolved suggestions hide their Accept/Reject controls (mirroring the status-line logic).
- **Verified:** CONFIRMED independently. Verified HistoryPanel.tsx:145-150 (selectedSuggestionAccepted derives from acceptedSuggestionIds; selectedSuggestionResolution from suggestionResolutions), :273 (status accounts for rejected) vs :293 (button gate ignores rejected resolution). EditorWorkspace.tsx:1103 records 'rejected' resolution without adding to acceptedSuggestionIds, so the gate inconsistency is real. Note the `if (currentText === targetText) return;` guard at line 1083 means a redundant re-reject is a no-op when text already matches parent, but re-Accept remains genuinely enabled, so the UX defect (buttons reappearing on a resolved suggestion) holds. issue_type=ux and severity=low are correct; suggested_fix is sound.

### [ ] F-C11-07 · Suggest share scope / capability gating `spec-error`

- **Cluster:** C11 · **Verdict:** partial
- **Problem:** The 'suggest' scope satisfies the story as literally written: SharePanel issues a suggest link (share-create-suggest, SharePanel.tsx:172-173), capabilitiesForScope('suggest') grants doc.read/history.read/mention.see/chip.preview + doc.comment + doc.suggest but NOT doc.edit, and the panel renders share-can-suggest=yes / share-can-comment=yes / share-can-edit=no. However the implied tiering (suggest as a step up from comment) does not exist: capabilitiesForScope('comment') returns the IDENTICAL capability set including doc.suggest, so 'comment' and 'suggest' scopes are functionally indistinguishable in capabilities. The differentiation between a comment link and a suggest link is cosmetic/labeling only.
- **Fix:** Either remove 'doc.suggest' from the 'comment' case in packages/rbac/src/index.ts:105-113 so suggest becomes a genuinely distinct capability tier, or update the story to note that comment and suggest scopes grant identical capabilities and the suggest link is a labeling convenience.
- **Verified:** CONFIRMED independently. packages/rbac/src/index.ts:105-122 shows the 'comment' case (105-113) and 'suggest' case (114-122) return byte-identical capability arrays, both including 'doc.comment' AND 'doc.suggest'. The scopes are functionally identical. SharePanel.tsx:271-273 confirms the yes/yes/no rendering. The story's literal ACs pass (hence verdict=partial), but the intended distinction is absent. issue_type=spec-error and severity=low are appropriate; both suggested fixes are valid.

### [ ] F-C12-08 · Periodic snapshots for faster restore `logistical`

- **Cluster:** C12 · **Verdict:** fail
- **Problem:** snapshotInterval config and the 'uses snapshot if available' loadAt doc comment are inert. createHistoryStore reads only config.now; config.snapshotInterval is never referenced anywhere in the codebase. The in-memory store materializes and stores the FULL doc state for every sha unconditionally (states.set(resultSha, cloneJson(resultDoc)) on every append), so there is no snapshot-interval logic and the option does nothing. The module header still marks history as a SCAFFOLD. restore/loadAt remain functionally correct, so the impact is only the unimplemented optimization, hence low severity.
- **Fix:** Either remove the snapshotInterval option and the 'uses snapshot if available' comment to avoid implying a feature that does not exist, or implement snapshotting: only persist full state every config.snapshotInterval entries and reconstruct intermediate states in loadAt by replaying ops from the nearest preceding snapshot. Drop the SCAFFOLD banner once history is considered shipped.
- **Verified:** CONFIRMED by independent code reading. Every claim reproduced: (1) grep across packages/ and apps/ shows snapshotInterval appears ONLY at index.ts:82 as a declaration, never read; createHistoryStore (line 152) uses only config.now (line 211). (2) index.ts:225 stores full cloned state unconditionally on every append. (3) loadAt (243-249) just does states.get(sha) with no snapshot/replay distinction, so the line 68 comment is unbacked. (4) SCAFFOLD header present at line 3. STORIES.csv:95 expected_behaviour independently describes this as a stub (story type='stub', state='story-drafted'/'pending'), so this is a known tracked stub rather than a hidden defect — which reinforces low severity and the logistical (docs-imply-nonexistent-feature) classification. issue_type=logistical, severity=low, and suggested_fix are all sound; suggested_fix lightly tightened for accuracy ('reads only config.now' rather than 'destructures only config.now', since config is passed whole).

### [ ] F-C13-05 · Copy filtered-diff share link `logistical`

- **Cluster:** C13 · **Verdict:** partial
- **Problem:** handleCopyHistoryFilterLink (EditorWorkspace.tsx:998-1021) builds a URL with historyAuthor/historyTime params from current filter state, mirrors it to window.\_\_lashLastHistoryFilterLink, and copies via copyToClipboard with an ok|failed fallback hook. EditorWorkspace never reads historyAuthor/historyTime back from the URL on mount, so opening a shared link does not auto-apply the filters; the round trip is one-directional.
- **Fix:** On mount, add a useEffect reading new URL(window.location).searchParams.get('historyAuthor'/'historyTime') and call setHistoryAuthorFilter / setHistoryTimeFilter so a shared link restores the filtered view. Note: historyTimeFilter is typed HistoryTimeFilter (not a free string), so validate/cast the parsed value before setting it.
- **Verified:** Code observations independently reproduced. Lines 1001-1007 are the ONLY references to these params and are all .delete/.set WRITES; grep confirms no searchParams.get for them, and state is initialized to null at 353-354. The 'one-directional round trip' is real. HOWEVER I downgraded severity medium->low: STORIES.csv:100 acceptance criteria EXPLICITLY document this as intended/known behavior ('NOTE: ... EditorWorkspace never reads historyAuthor/historyTime back from the URL on mount, so opening a shared link does not auto-apply the filters'). Since the implemented behavior matches the written spec, this is a spec-design gap / enhancement rather than a deviation from acceptance criteria. issue_type 'logistical' is apt (missing round-trip wiring). The suggested_fix is sound; I added a caveat that historyTimeFilter is a typed value (HistoryTimeFilter), so the parsed query string should be validated before setHistoryTimeFilter.

### [ ] F-C13-06 · Redacted diff for shared links `logistical`

- **Cluster:** C13 · **Verdict:** partial
- **Problem:** redactDiff (packages/share/src/index.ts:162-177) returns the diff unchanged when policy.rules is empty; otherwise it replaces text of every inserted/deleted span with '[redacted]' (redacted:true) while leaving unchanged/attrChanged spans and all positions/ids intact. Redaction is all-or-nothing keyed solely on policy.rules.length being non-empty: the rule contents (path, action) are never consulted and callerId is unused (renamed \_callerId). Any non-empty policy masks ALL inserted/deleted text regardless of which paths the rules target, their action ('redact'|'omit'|'hash'), or the caller.
- **Fix:** If selective redaction is intended, evaluate each policy rule's path/action against spans (note rules carry 'redact'|'omit'|'hash' actions the current impl ignores, treating all as a blanket redact). If uniform redaction is the spec, document that policy.rules acts purely as an on/off flag and callerId is reserved-but-unused. NOTE: the original suggested fix mentioned matching 'authorIds/callerId' on rules, but RedactionPolicy.rules is typed Array<{path; action}> with no author/caller fields, so per-caller matching would require a type change.
- **Verified:** Code observations independently reproduced exactly: line 167 only checks policy.rules.length; the span map ignores rule path/action; the third arg is \_callerId (unused). RedactionPolicy.rules is Array<{path:string; action:'redact'|'omit'|'hash'}> (index.ts:25-30) — confirming the rule contents (and the omit/hash actions) are never honored. share-rbac.test.ts:109-117 only asserts the blanket-redact path. I downgraded severity medium->low because STORIES.csv:101 acceptance criteria EXPLICITLY state the implemented behavior: 'redaction is applied uniformly to text spans regardless of the specific rule paths or the callerId (callerId is unused).' The code matches the written spec, so this is a spec/design limitation, not a deviation. I corrected the suggested_fix: the original referenced matching 'authorIds/callerId' on rules, but no such fields exist on the rule type — the more accurate gap is that the 'omit'/'hash' actions and per-path scoping in the type are silently collapsed to a single redact behavior.

### [ ] F-C14-03 · Record authorship from a history entry `logistical`

- **Cluster:** C14 · **Verdict:** partial
- **Problem:** recordEntry (packages/authorship/src/index.ts:132-145) calls applyOps(entry.ops) at :133 to remap existing intervals through the full sequential op sequence, then at :134-144 iterates entry.ops and for each non-empty replace_text put()s a new interval [op.from, op.from+text.length) using the RAW op coordinates. Op coordinates within a single HistoryEntry are sequential (each op's from/to index into the document state produced by prior ops) — confirmed by the canonical consumer replayOps in packages/history/src/index.ts:386-398, which applies each replace_text/delete_range to the already-mutated state. The put() loop does not re-map the new interval through the ops that follow it in the same entry, so for a multi-op entry the inserted text for ops at index>0 can be placed at the wrong final coordinates. Single-op entries (the dominant in-app paths at EditorWorkspace.tsx:779 and :1089) are unaffected; the edge only bites multi-replace_text/delete_range entries (e.g. AI patch.operations replayed at :1158).
- **Fix:** Interleave the remap and attribution inside recordEntry: for each op, run applyTextReplace for that single op, then put() its new interval, then proceed to the next op — so each new interval is subsequently mapped by later ops. Equivalently, track a running net-delta of inserts/deletes from later ops and offset the put() coordinates. Keep applyOps as a whole-sequence remap for pre-existing intervals.
- **Verified:** CONFIRMED. Independently reproduced from code. applyOps (index.ts:86-94) and replayOps (history/src/index.ts:386-398) both apply ops sequentially, establishing that op[i].from lives in the post-op[0..i-1] coordinate space; the put() loop bypasses any mapping for ops that follow each new interval, so index>0 intervals can land at wrong final positions. Severity low is correct: all in-app history append paths I inspected emit single-op entries, so the bug is latent and only triggers on multi-op (AI EditPatch) entries. issue_type logistical is acceptable (could equally be 'bug', but it is a faithful derivation of the spec text which itself describes the raw-op-from implementation, so the residual is an implementation correctness edge rather than a spec deviation — keeping 'logistical'). One correction to the original mechanism wording: the drift is NOT from 'earlier ops in the same entry' (op[i].from is already authored in the post-earlier-op space, so earlier ops are accounted for); the real residual is that LATER ops in the same entry shift the freshly put() interval. The suggested_fix (interleave applyTextReplace+put per op, or running-offset) is sound and I tightened it accordingly.

### [ ] F-C15-05 · User and AI replies within a thread `ux`

- **Cluster:** C15 · **Verdict:** partial
- **Problem:** Reply input is bound to per-thread replyDrafts; Reply button disabled until draft non-blank; submit trims body, appends a local-user message and clears that thread's draft. 'Add AI reply' appends an AI_USER message ('ai-doc-chat', 'AI Doc Chat') with hardcoded body 'AI reply with current document context.' Both bump updatedAt. Messages render data-author-id/type. As the spec itself flags, the AI reply is a hardcoded placeholder, not a real model call — story is correctly marked 'partial'.
- **Fix:** Acceptable for current scope. To close the gap, wire addAiReply to a real model/citation endpoint and surface a loading/error state on the AI button instead of synchronously inserting a canned string.
- **Verified:** Independently reproduced every claim in ChatPanel.tsx. addAiReply (line 338-340) calls addReply(threadId, AI_USER, 'AI reply with current document context.') — literal hardcoded string, no model call, no citations attached despite the ChatMessage.citations field existing in doc-chat/index.ts. AI_USER = { type: 'ai', id: 'ai-doc-chat', label: 'AI Doc Chat' } at line 31. Reply input value={replyDraft} bound to replyDrafts[thread.id] (lines 416/474), button disabled={!replyDraft.trim()} (line 486), addUserReply clears the per-thread draft (line 335), addReply trims and no-ops on blank (lines 309-310). Messages render data-author-id and data-author-type (lines 462-463). Both add paths bump updatedAt. The placeholder is a real (acknowledged) functional gap, not a crash, so severity=low / issue_type=ux is correct and verdict 'partial' is the honest grade. Suggested fix is sound. confirmed=true.

### [ ] F-C15-08 · In-memory ThreadStore (create/reply/list) backend `logistical`

- **Cluster:** C15 · **Verdict:** partial
- **Problem:** createThreadStore returns create (mints `thread:N` with cloned anchor, open status, empty messages), reply (mints `message:N`, pushes, throws 'unknown thread' if id missing), and list (deep-cloned threads for a docId with optional authorId/ai filtering). The config { adapter } is accepted but ignored (param prefixed \_config) — only an in-memory Map exists, no postgres path. The ChatPanel UI does not use this store at all; it manages threads directly via React state + storage. Story is correctly marked 'partial'.
- **Fix:** Acceptable as a defined backend contract for tests. To fully close: either implement the postgres adapter branch (currently dead config) or document the in-memory-only limitation, and decide whether the UI should consume this store rather than duplicating thread logic.
- **Verified:** Verified against doc-chat/index.ts. createThreadStore signature is (\_config: { adapter: 'memory' | 'postgres' }) with the underscore-prefixed param confirming it is intentionally unused — the body only constructs a `const threads = new Map` (line 142); there is no branch on adapter, so the 'postgres' value is dead config. create mints `thread:${threadSeq}` (line 150) with cloned anchor (152), status 'open', resolvedAt null, empty messages. reply mints `message:${messageSeq}` (174) and throws `doc-chat.reply: unknown thread ${threadId}` (169) when missing. list filters by docId then by authorId (some message.author.id match) or ai (some/every author.type), returning deep clones of anchor/token/messages (194-198). Confirmed ChatPanel.tsx line 6 imports only { createAnchor, mapAnchor, type ChatMessage, type ChatThread } from '@lash/doc-chat' — createThreadStore is never imported or called; the panel duplicates thread logic via React state, localStorage (writeThreadsToStorage), and a Yjs map (writeThreadsToYMap). This is a logistical/design gap (unused backend + ignored adapter config), not a runtime defect, so severity=low / issue_type=logistical and verdict 'partial' are accurate. Suggested fix is reasonable. confirmed=true.

### [ ] F-C16-03 · Share-link revocation `logistical`

- **Cluster:** C16 · **Verdict:** partial
- **Problem:** signer.revoke(jti, revokedBy, reason) silently no-ops for any jti not in the per-instance in-memory `issued` Map (early return on line 144), so a freshly constructed signer cannot revoke a token it did not itself issue. The localStorage-backed invite revocation path (revokeInviteRecord) sidesteps this by writing directly to the persistent RevocationStore. The web SharePanel never calls signer.revoke; it only revokes invites.
- **Fix:** Make signer.revoke independent of the in-memory issued Map: write the RevocationRecord/audit event based on jti (and an optional docId arg) unconditionally, or look the token up via the revocations/policies stores. The localStorage invite path already does the right thing; align signer.revoke with it. (If docId is needed for the audit event, accept it as a parameter since it can no longer be read off the in-memory token.)
- **Verified:** CONFIRMED by code. packages/share/src/index.ts:142-144 reads `const token = issued.get(jti); if (!token) return;` — a silent no-op when the jti was issued by a different signer instance. validate() reconstructs the token from the payload (line 120: `issued.get(payload.jti) ?? { ...payload, token }`), so revoke is the only path still bound to the in-memory Map. inviteAccess.ts:143-157 confirms revokeInviteRecord writes directly via createLocalRevocationStore(storage).revoke(...) bypassing signer.revoke, and SharePanel.tsx:140-145 confirms revokeInvite calls revokeInviteRecord only. Severity low is appropriate: the invite path (the only one used by the UI) works correctly, this is a latent inconsistency / API foot-gun in the signer itself. issue_type logistical is correct. Note that the suggested fix must account for docId, which is currently sourced from the in-memory token for the audit event; I added that to the fix.

### [ ] F-C18-04 · Stale base-version rejection on apply `logistical`

- **Cluster:** C18 · **Verdict:** partial
- **Problem:** handleApplyAiPatch correctly rejects stale patches at apply time: patch.baseVersion !== historyHeadRef -> {ok:false, reason:'base version stale'}, plus guards for missing editor/history and unflushed local edits, and propagates history append failures. However the ValidationResult type defines a 'base-version-stale' reason that validateEditPatch never produces (the validator receives baseDoc but never hashes/compares it to baseVersion). So the stale check lives only in the host apply path, not in the pure validator, leaving a declared-but-unimplemented validator branch (dead reason).
- **Fix:** Either implement hashCanonical(baseDoc) comparison against patch.baseVersion inside validateEditPatch to emit 'base-version-stale', or remove the unused reason from the union and document that staleness is enforced solely at the host apply boundary.
- **Verified:** CONFIRMED. EditorWorkspace.tsx:1141-1149 implements the apply-time stale guard exactly as described (expectedParentSha = historyHeadRef.current; mismatch -> 'base version stale'; plus 'history unavailable' and 'pending local history flush' guards; append failures propagated at result.ok check). packages/ai/src/index.ts:21 declares 'base-version-stale' in the ValidationResult union, and validateEditPatch (lines 148-186) never reads options.baseDoc nor any hash helper (no hashCanonical exists anywhere in the repo) -- it only checks author, schemaVersion, op count, schema-safety, and selection scope. So 'base-version-stale' is genuinely a dead/unreachable validator reason. Issue_type 'logistical' and severity 'low' are appropriate: the real runtime guarantee is met at the host boundary; this is a code-hygiene/dead-branch concern, not a functional defect. Suggested fix is sound (both options valid).

### [ ] F-C19-06 · Accessible autosave live region `a11y`

- **Cluster:** C19 · **Verdict:** partial
- **Problem:** The indicator uses role="status" aria-live="polite" so subsequent status text changes are announced. However, the component returns null in the idle state (AutosaveIndicator.tsx:39-41), so the live-region span (lines 56-66) is absent from the DOM until the first transaction, then inserted simultaneously with its first content (status becomes 'pending' → 'Unsaved changes'). Screen readers generally do not reliably announce a live region whose element is added to the DOM at the same time as its initial content; the region must already exist and be monitored beforehand. The very first 'Unsaved changes'/'Saving…' announcement may therefore be missed.
- **Fix:** Always render the live-region span as an empty, visually-hidden <span role="status" aria-live="polite"> that stays mounted regardless of status, and only update its text content / visibility based on status (returning empty text instead of null in idle). This ensures assistive tech is already monitoring the region before the first status change so the initial announcement is not dropped.
- **Verified:** CONFIRMED. Reproduced directly from the code. AutosaveIndicator.tsx:39-41 returns null while status==='idle', and the only element carrying role="status"/aria-live="polite" is the span at lines 56-66, which is rendered for the first time when status first becomes non-idle ('pending', label 'Unsaved changes' at line 49). useAutosave (autosave.ts:246) starts in status 'idle', and notify() (autosave.ts:132) sets status to 'pending' on the first transaction, confirming the live region's element and its first text content appear in the same render commit. The described screen-reader behavior — that a live region inserted together with its first content is often not announced because it was not being monitored beforehand — is well-documented and correct (applies to NVDA/VoiceOver/JAWS in common modes). The suggested fix is sound and is the standard remedy; I slightly clarified it to specify returning empty text (not null) in the idle branch so the element stays mounted. Severity 'low' is appropriate: this is a polite region affecting only the first announcement, no visual impact, and all later transitions (saving/saved/error) still announce correctly since the region persists once mounted. issue_type 'a11y' and verdict 'partial' are correct.

### [ ] F-C20-04 · Multi-client CRDT convergence via collab-service `spec-error`

- **Cluster:** C20 · **Verdict:** fail
- **Problem:** The user story asks for edits from separate browser sessions to merge into one converging document, but @lash/collab-service is an explicit local scaffold: createLocalCollabRoom only JSON-encodes EditorOp[] into a Uint8Array and emits to handlers within the current JS process. It does not import Yjs, open a websocket, or persist/sync across browsers, so genuine multi-client convergence is NOT delivered by this module. Cross-client convergence is provided instead by LashRealtimeYjsProvider (F-C20-05), not collab-service.
- **Fix:** Mark this story explicitly as scaffold/non-shipping and point multi-client convergence to F-C20-05's Yjs WebSocket provider; or replace the in-process emit with a real Yjs Y.Doc + transport if collab-service is meant to deliver convergence.
- **Verified:** CONFIRMED. Every code claim independently reproduced from packages/collab-service/src/index.ts. Header lines 1-19 explicitly state 'LOCAL SCAFFOLD... does not import Yjs, open a websocket, persist updates, or synchronize separate browsers yet.' The only import (line 21) is from @lash/types -- no Yjs. encodeOps/decodeOps (lines 75-82) use TextEncoder/JSON to serialize EditorOp[]. applyOnline (lines 101-104) pushes to a local appliedOps array and invokes only in-process updateHandlers; nothing leaves the JS process. createCollabRoom (lines 174-176) just returns createLocalCollabRoom. STORIES.csv line 151 confirms F-C20-04 is the convergence story (status 'stub') and line 152 confirms F-C20-05 is the real Yjs-over-WebSocket provider LashRealtimeYjsProvider. So the redirect to F-C20-05 is accurate. NOTE on framing: the story's expected_behaviour field is itself honest and already documents the scaffold limitation, and status is 'stub' -- the spec is internally consistent rather than deceptive, so this is a story-scoping/labeling issue, not a hidden defect. issue_type 'spec-error', verdict 'fail', and severity 'low' are all appropriate, and the suggested_fix is sound (label as scaffold + redirect to F-C20-05). Confirming as flagged.

### [ ] F-C21-03 · Room session token issuance (invite-based + local fallback) `spec-error`

- **Cluster:** C21 · **Verdict:** partial
- **Problem:** Session issuance largely matches the story: with an inviteToken it verifies via inviteSecret and mints a scope-derived grant (view => doc.read, else doc.read+doc.edit), denying with the access reason on failure; without a token it issues a default doc.read+doc.edit grant only under local fallback, else denies 'invalid'; success returns {ok:true, accessToken, grant, inviteRequired:!usingLocalFallback} with 1h-expiry grants carrying issuedAt. However, the host gate at index.ts:112 (`if (usingLocalFallback && !isLocalRealtimeFallbackHost(url)) return deny('invalid')`) runs BEFORE the invite branch (index.ts:116), so it applies to BOTH the no-token and invite-token paths. The story scopes the localhost requirement to the no-token fallback path only. Consequently a deployed worker that has not configured LASH_REALTIME_SESSION_SECRET will reject an otherwise-valid invite token from a non-local host with reason 'invalid' rather than honoring it. Blast radius is small (production normally sets a secret, which makes usingLocalFallback false and disables the gate), so this is a spec/ordering mismatch rather than a hard defect.
- **Fix:** Move the `usingLocalFallback && !isLocalRealtimeFallbackHost` check inside the no-token branch (place it in the `else if (usingLocalFallback)` arm, before createDefaultRealtimeGrant, so non-local hosts fall through to deny('invalid')) so the host restriction only gates the local dev fallback and never blocks a verified invite token. Alternatively, correct the story to state the host gate applies to all secret-less requests.
- **Verified:** CONFIRMED by reading the code. index.ts:112-114 places the host gate before the inviteToken branch (index.ts:116-124); sessionSecret() sets usingLocalFallback=!configured (index.ts:51-56); isLocalRealtimeFallbackHost only matches localhost/127.0.0.1/[::1] (routing.ts:37-38). STORIES.csv:157 explicitly scopes the localhost requirement to the no-token path ('Without a token: if no configured session secret ... AND host is localhost ... on non-local hosts ... returns 403 invalid'), with no such restriction stated for the invite-token path. Therefore, with no session secret configured, a valid invite token from a non-local host is denied before verification — a real ordering/spec mismatch. severity=low and issue_type=spec-error are both accurate: in production LASH_REALTIME_SESSION_SECRET is set, making usingLocalFallback false and the gate inert, so impact is limited to a misconfigured/secret-less deployment. The invite path already does its own verification+denial (access.ts:286-302, createRealtimeGrantFromInviteToken), so the recommended narrowing of the gate to the no-token branch is safe and is the cleaner of the two suggested fixes.

### [ ] F-C25-06 · Document registry persistence & listing `spec-error`

- **Cluster:** C25 · **Verdict:** partial
- **Problem:** listDocuments seeds demo-document, merges parsed records (parseRegistry tolerates malformed/non-array JSON by returning []), overlays live titles, and sorts demo-first/updatedAt-desc; upsertDocument preserves createdAt and bumps updatedAt. However the story claims 'a storage-change effect re-reads the list into React state so the switcher stays current' — there is NO window 'storage' listener for the document registry (lash:documents). The only storage listener is for suggestion resolutions. So the switcher updates only on local mutations (create/title-edit via refreshDocuments) and not on cross-tab registry changes.
- **Fix:** Either correct the story text (drop the cross-tab claim) or add a useEffect registering window.addEventListener('storage', e => { if (e.key === DOCUMENT_REGISTRY_STORAGE_KEY || e.key?.startsWith('lash:title:')) refreshDocuments(); }) so the switcher reflects edits made in other tabs.
- **Verified:** CONFIRMED via code. EditorWorkspace.tsx:454-458 is the sole 'storage' listener and it is keyed on suggestionResolutionStorageKey(historyDocumentId) — it never reacts to DOCUMENT_REGISTRY_STORAGE_KEY ('lash:documents') or 'lash:title:\*'. refreshDocuments (391-394) is only invoked on local mutations/mount (488,494). STORIES.csv:187 expected_behaviour explicitly asserts 'a storage-change effect re-reads the list into React state so the switcher stays current,' which is false for the registry — so this is primarily a spec-error (the story documents behavior that does not exist), with a secondary minor logistical gap (no cross-tab sync). I reclassified issue_type from 'logistical' to 'spec-error' because the load-bearing problem the finding identifies is the story's inaccurate claim; cross-tab sync is a nice-to-have, not a stated product requirement (the user story is just 'documents remembered'). Severity low is correct: single-tab usage works fully. Suggested_fix adjusted to offer the spec-correction option first.

### [ ] F-C25-12 · Document metadata strip `ux`

- **Cluster:** C25 · **Verdict:** partial
- **Problem:** The metadata row (data-testid lash-doc-meta, aria-label 'Document metadata') renders 'Edited by Apollo' (HARD-CODED, not derived from real authorship), pluralized version count (historyEntries.length), pluralized section count (outlineItems.length), the live route (documentPath via lash-doc-route), and the invite/access status (Owner access / Access granted:<scope> / Denied:<reason>). All values are real except the authorship line, which is a static placeholder presented as factual metadata.
- **Fix:** Replace the hard-coded 'Edited by Apollo' with the real last-editor/author (e.g. from authorship/history state or the active actor) or remove it until authorship attribution is available.
- **Verified:** CONFIRMED via code. EditorWorkspace.tsx:1642 literally renders <span>Edited by Apollo</span> with no data binding, while the sibling spans derive from real state: version count from historyEntries.length (1645), section count from outlineItems.length (1649), route from documentPath(activeDocumentId) (1652), and invite access from inviteAccess (1654-1660). STORIES.csv:193 admits 'Edited by Apollo (hard-coded)'. So the line is a static placeholder presented as factual authorship metadata. Low severity is correct — it is one cosmetic/misleading field among otherwise-real metadata. Suggested_fix is sound.
